export interface Contact {
    id: number;
    first_name: string;
    last_name: string;
    birthday: string;
    notes ?: string;
    phone ?: string;
    email ?: string;
    address ?: string;
}

